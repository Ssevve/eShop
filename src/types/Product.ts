interface Product {
  id: string;
  productName: string;
  brand: string;
  price: number;
  discountPrice: number;
  imageUrl: string;
  quantity: string;
  category: string;
  subCategory: string;
}

export default Product;
